#include "main.h"
#include <string.h>

/**
 * _strlen - this function returns the length of a string
 * @s: the parameter of char type
 *
 * Return: void
 */

int _strlen(char *s)
{
	int i;

	i = strlen(s);

	return (i);
}
