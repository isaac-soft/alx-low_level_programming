#include "main.h"
#include <stdio.h>

/**
 * mul - returns the multiplication of two numbers
 *
 * @a: - 11st parameter
 * @b: -secondparameter
 *Return: 
 */

int mul(int a, int b)
{
	return (a * b);
}
